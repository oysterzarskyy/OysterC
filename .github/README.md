# GitHub Markdown Notes

Use fenced code blocks with `c` or `cpp` syntax for OysterC examples so GitHub renders them with C-style coloring.

Example:

```c
#include <stdio.h>

int main(void) {
    puts("Hello from OysterC");
    return 0;
}
```
